// wywola funkcje fn z argumentami arg1 oraz arg2, w kontekscie obiektu contextObj
function callInContext(fn, contextObj, arg1, arg2) {
}

// Wywola funkcje fn z dowolna iloscia pozostalych argumentow, w kontekscie obiektu contextObj
// Przyklady:
// - applyFunctionWithArgs(fn, contextObj)
// - applyFunctionWithArgs(fn, contextObj, 1, ["qq"], null, {name: "Bob"})
function applyInContextWithArgs(fn, contextObj) {
}
