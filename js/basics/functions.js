// Zwraca string zawierajaca argumenty z jakimi funckaj zostala wywolana
// argsToArray(1, null, "imba") => "1, null, imba"
function argsToString() { // convert arguments to string
}

// Zwraca tablice (obiekt klasy Array) zawierajaca argumenty z jakimi funckaj zostala wywolana
// argsToArray(1, null, "imba") => [1, null, "imba"]
function argsToArray() {
}

// Wywola funkcje fn z parametrami name i age
// (fn, name, age) => fn(name, age)
function callFunctionWithParams(fn, name, age) {
}

// Wywola funkcje fn z dowolna iloscia pozostalych parametrow
// (fn, arg1, arg2, ..., argN) => fn(arg1, arg2, ...,  argN)
function applyFunctionWithArgs(fn) {
}
